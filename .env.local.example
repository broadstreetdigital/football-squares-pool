# Local Development Environment Variables
# Copy this to .env.local for local development

# JWT Secret (required - generate with: openssl rand -base64 32)
JWT_SECRET=local-dev-secret-replace-in-production-min-32-chars

# Node Environment
NODE_ENV=development

# Optional: Base URL (Webflow Cloud sets this automatically)
# BASE_URL=

# Optional: Assets Prefix (Webflow Cloud sets this automatically)
# ASSETS_PREFIX=
