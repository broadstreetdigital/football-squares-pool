# Application Settings
NODE_ENV=development

# JWT Secret (REQUIRED in production - generate with: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-min-32-chars

# Webflow Cloud Environment Variables (set automatically by Webflow)
# BASE_URL=/app
# ASSETS_PREFIX=https://cdn.webflow.io/your-site

# Database (optional - Webflow Cloud provides SQLite binding by default)
# DATABASE_PATH=./data/football-squares.db
