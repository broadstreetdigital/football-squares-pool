# Application Settings
NODE_ENV=development

# JWT Secret (REQUIRED in production - generate with: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-min-32-chars

# Webflow Cloud Environment Variables
# BASE_URL=/app (for server-side routing - set automatically by Webflow)
# ASSETS_PREFIX=https://cdn.webflow.io/your-site (set automatically by Webflow)
# NEXT_PUBLIC_BASE_PATH=/app (REQUIRED for client-side API calls - must match BASE_URL)

# Database (optional - Webflow Cloud provides SQLite binding by default)
# DATABASE_PATH=./data/football-squares.db
